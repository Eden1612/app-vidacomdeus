import React from 'react';
import { SafeAreaView, View, Text } from 'react-native';

const NoteteScreen = () => {
    return(
        <SafeAreaView>
            <Text>Estou funcionando</Text>
        </SafeAreaView>
    );
}

export default NoteteScreen;